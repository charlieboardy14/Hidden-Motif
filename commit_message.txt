feat: Add TypeScript type annotations to App.tsx

This commit resolves TypeScript errors related to implicit any types and JSX elements by adding explicit type definitions for component props and game state.